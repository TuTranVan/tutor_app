<div class="login-form">
  <%= form_for @student do |f| %>
    <%= render "shared/error_messages", object: @student %>

    <div class="avatar">
      <%= image_tag("logo.png") %>
    </div>
    <h2 class="text-center">NHẬP THÔNG TIN CÁ NHÂN</h2>

    <%= f.hidden_field :user_id, value: current_user.id %>

    <div class="form-group">
      <%= f.text_field :name, class: "form-control", placeholder: "Họ tên" %>
    </div>

    <div class="form-group">
      <%= f.text_field :phone, class: "form-control", placeholder: "Điện thoại di dộng" %>
    </div>

    <div class="form-group">
      <%= f.text_field :address, class: "form-control", placeholder: "Địa chỉ" %>
    </div>

    <div class="form-group">
      <%= f.date_field :dob, class: "form-control" %>
    </div>

    <div class="form-group">
      <%= f.radio_button :gender, "Nam", checked: true %>
      <%= f.label :gender, "Nam", value: "Nam", class: "control-label" %>
      <%= f.radio_button :gender, "Nữ", class: "control-label" %>
      <%= f.label :gender, "Nữ", value: "Nữ" %>
    </div>

    <div class="form-group">
      <%= f.label :literacy, class: "control-label" %>
      <%= f.select:literacy, options_for_student, class: "form-control" %>
    </div>
    <div class="form-group">
      <%= f.submit yield(:button_text), class: "btn btn-primary btn-lg btn-block login-btn" %>
    </div>
    <p class="text-center small"><%= link_to "Hủy đăng ký!", user_path(current_user), method: :delete %></p>
  <% end %>
</div>
